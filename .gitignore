# Dépendances
web/node_modules/

# Build Next.js (tous les dossiers .next, y compris les copies numérotées)
web/.next/
web/.next-*/
web/(2).next/
web/out/

# Secrets — ne jamais committer
web/.env.local
web/.env*.local

# Divers
.DS_Store
Thumbs.db
TEMP/
